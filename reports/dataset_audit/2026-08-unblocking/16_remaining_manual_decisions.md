# 尚需人工决定的事项

1. **CasinoLimit候选是否可信：**人工复核45条优先队列，给出accept/reject/uncertain和anchor子集；若精确率不足，停止扩大标注。
2. **是否联系数据作者：**请求CasinoLimit地址/方向/端口语义或直接标签映射，以及UWF公开论文所述mission log/activity映射。
3. **数据补充分支：**优先等待上述官方元数据，还是授权对CAM-LDS单个run进行直接NetFlow连接probe；本轮未下载候选大文件。
4. **任务收缩条件：**若只能确认`T1046`等极少类别，是否接受转为可观察性/能力边界研究，而非广覆盖开放Technique识别。
5. **工程并行范围：**是否在数据Gate关闭期间单独安排RTX 5090加载与单步反传冒烟；该工作不产生正式训练结论。

这些决定不能由测试结果代替，也不能用Flow行数、启发式UWF时间簇或RAG文本自动补齐。
